# 工作记录 2026-07-27

## 一、方案层命中率统计（已上线 ✅）

全流程：brainstorming → 规格 → 实施计划 → 子代理执行（每任务规格+质量双审）→ 终审 → push。

- **需求**：方案块（胜平负/进球数/比分/过关串关/亚洲让球/大小盘）追踪红黑，块级手动回填 `result`（hit/miss/half/push）
- **stats.js**：`planTypeOf()` 关键词归 6 类 + `planStats()` 统计（半红0.5、走水/未回填不计分母、last14 逐块升序≤14）；30+ 测试断言
- **index.html**：主区「🎯 方案层命中」面板（X红X黑+命中率[样本<3 显示样本不足]+迷你圆点）；方块/日卡/弹窗三处徽标 🔴⚫🌗➖；旧缓存防御 guard；hasOwnProperty 硬化
- **数据**：7/26 六块已回填（胜平负2半/比分1黑/过关1黑/亚让1半/大小1半），7/27 待赛果
- **文档**：spec/plan/README 三方口径一致；dist/ 已同步
- **教训**：计划里 Task4 预期输出曾把过关专栏重复计入胜平负，被实施子代理抓出——审查流程起效
- 提交：7163100 → 63919ed（已 push）

## 二、会员注册登录门控（设计完成，待实施 ⏸）

- **需求**（brainstorming 确认）：自助注册（邮箱+密码）→ 站长审核 → 全站门控；小圈子分享目的
- **方案**：Supabase（Auth + Postgres + RLS），GitHub Pages 继续托管静态壳
  - `members.approved` 审核位 + `prediction_days` 数据表；RLS 服务端强制，anon key 读不到
  - 前端三态门：登录/注册 → 等开门 → 全站内容（现有渲染包 `renderApp(days)`）
  - `predictions.js` 移出公开仓库 + `tools/sync-data.js` 同步上传（service key 本机 .env）
  - 每日工作流变：改文件 → 测试 → `node tools/sync-data.js`（壳没改不用 push）
- **设计文档**：`docs/superpowers/specs/2026-07-27-membership-gate-design.md`（bf0820e），待用户审阅后进 writing-plans
- **实施前置**：需站长注册 Supabase 账号建项目（区域 Singapore），拿到 URL + anon key + service key

## 三、明日待办

- [ ] 回填 7/27 两场赛果（赫根vs索尔纳、罗森博格vs腓特烈斯塔）+ 复盘 + 方案块 result
- [ ] 门控规格审阅 → 实施计划 → 实施（需 Supabase 账号）
- [ ] 7/28 竞彩分析
